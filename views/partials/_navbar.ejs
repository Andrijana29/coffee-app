<nav class="navbar">
  <div class="nav-container">
    <ul class="nav-links">
      <a href="/">Home</a>  
      <% if(user) { %>
        <a href="/users/<%= user._id %>/coffees">Your Coffees</a>
        <a href="/auth/sign-out">Sign Out</a>
      <% } else { %>
        <a href="/auth/sign-in">Sign In</a>
        <a href="/auth/sign-up">Sign Up</a>
      <% } %>
    </ul>
  </div>
</nav>